class DbClientMyself():
    # def __init__():
    #     raise Exception('该DBClient方法尚未重写')

    def save(self, missionBean, *args):
        raise Exception('该DBClient方法尚未重写')

    def select(self, **kwargs):
        raise Exception('该DBClient方法尚未重写')
